apiVersion: apps/v1
kind: Deployment
metadata:
  name: document-service
  namespace: test
spec:
  replicas: 1
  selector:
    matchLabels:
      app: document-service
  template:
    metadata:
      labels:
        app: document-service
    spec:
      containers:
      - name: document-service
        image: 922805825674.dkr.ecr.us-east-1.amazonaws.com/document_service:latest
        ports:
        - containerPort: 8000
---
apiVersion: v1
kind: Service
metadata:
  name: document-service
  namespace: test
spec:
  selector:
    app: document-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: ClusterIP
