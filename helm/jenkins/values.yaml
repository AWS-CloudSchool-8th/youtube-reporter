controller:
  admin:
    username: admin
    password: admin

  serviceType: ClusterIP

  ingress:
    enabled: false

  resources:
    requests:
      cpu: "500m"
      memory: "1Gi"
    limits:
      cpu: "1"
      memory: "2Gi"

  image:
    tag: "lts-jdk17"

  serviceAccount:
    create: true
    name: jenkins-sa

persistence:
  enabled: true  # 임시로 비활성화
  size: 30Gi
  existingClaim: ""
  storageClass: "gp2-csi"
